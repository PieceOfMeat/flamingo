{
  "name": "flamingo-game",
  "version": "0.1.0",
  "description": "Browser scrolling shooter: pilot the Ukrainian Flamingo missile, dodge Russian interceptors and absurd obstacles.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
